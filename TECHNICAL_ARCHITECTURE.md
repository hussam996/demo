# المعمارية التقنية

## التقنيات

- **TypeScript + Vite** — بناء وتطوير.
- **Babylon.js 7** — الرندر. يحاول `WebGPUEngine` أولًا ثم يسقط تلقائيًا إلى WebGL2.
- **HTML/CSS** — كل الواجهات (HUD، بطاقة الطلب، القوائم) خارج المشهد ثلاثي الأبعاد.
- **LocalStorage** — حفظ التقدم بإصدار + Migration (مع Fallback في الذاكرة للاختبارات).
- **Vitest** — اختبارات منطق اللعبة. **Playwright** — اختبارات واجهة اللعب.
- **ESLint + Prettier** — جودة الكود.

## الفصل الأساسي: منطق ↔ رندر

القاعدة الصلبة في المشروع: **منطق اللعبة لا يعرف شيئًا عن Babylon**.

```text
gameplay/LevelSession  ←— tick(dt) —— GameplayScene (Babylon)
        |                                   ↑
        └── EventBus (أحداث typed) ————————┘
                 └——————→ ui/* (HUD, OrderCard, ...)
```

- `LevelSession` هي قلب اللعبة: آلة الحالة، الطابور، الصبر، التحقق، النقاط.
  تتقدم فقط عبر `tick(dt)` — لذلك هي حتمية وقابلة للاختبار بلا متصفح.
- الرندر والواجهة يشتركان في الأحداث (`customer-spawned`, `scoop-added`,
  `delivery-result`, …) ويستدعيان hooks مثل `notifyCustomerArrived(id)`
  عندما تنتهي حركة المشي — فالمنطق لا يفترض مدة الحركات.
- بيانات المحتوى (نكهات/صوصات/إضافات/أوعية/زبائن/مستويات) ملفات بيانات صرفة
  في `src/data/` و`levelConfigs.ts` — إضافة محتوى لا تلمس المنطق ولا الرندر.

## آلة الحالة

`GameplayState`: Loading → LevelIntro → WaitingForCustomer → TakingOrder →
PreparingOrder → DeliveringOrder → ValidatingOrder → CustomerReaction → …
مع LevelPaused / LevelCompleted / LevelFailed.

القيود مفروضة في `LevelSession` (وليس في الواجهة): لا إضافة مكونات إلا في
`PreparingOrder`، لا تسليم بمنصة فارغة، لا سكوب بلا وعاء، حد السكوبات حسب
سعة الوعاء والمستوى (`PreparationStation`).

## التحقق من الطلب

`validateOrder(order, prepared, options)` يقارن الوعاء والسكوبات (كمجموعة أو
كتسلسل حسب `requireScoopOrder`) والصوص والإضافات، ويتسامح مع الزوائد حسب
`extraIngredientsAllowed`. يعيد قائمة أخطاء typed + `scoreMultiplier`
(خطأ الترتيب وحده = 0.5 كرصيد جزئي في المستويات المتساهلة).

## الرندر

- **CartBuilder**: عربة كاملة إجرائيًا (هيكل، عجلات بمحور وبراغٍ، مقبض دفع، مظلة
  مخططة بقماش Dynamic Texture، لوحة اسم، أحواض مدمجة، رف أوعية، صوصات، برطمانات،
  منصة تحضير، جرس، سلة تفريغ). يعيد خريطة مواقع الأحواض وAnchors للتحضير والتسليم،
  و`applyLevel()` لإظهار/قفل المكونات حسب المستوى.
- **IceCreamVisual**: المنتج يُبنى هرميًا (وعاء ← سكوبات بمواقع محسوبة ← قمة).
  الصوص أنابيب (Tubes) على مسار حلزوني يعانق سطح الكرة + خطوط جانبية + قطرات.
  الإضافات تُوزّع بإحداثيات كروية على النصف العلوي مع محاذاة للـnormal
  (لا تطفو ولا تخترق). سقوط السكوب بـSquash & Stretch.
- **CustomerRenderer**: شخصيات إجرائية متنوعة (بشرة/شعر/ملابس/قبعات/نظارات/أحجام
  من `CUSTOMER_PALETTE`)، مشي عبر Waypoints مع دوران تدريجي وBobbing، مزاج
  بأيقونات Billboard، وObject Pooling كامل.
- **EnvironmentBuilder**: رمال، بحر خلفي بموجات متحركة، غيوم منجرفة، 6 نخلات،
  ممشى خشبي (مسار الزبائن)، مظلات وكراسٍ بعيدة، نوارس محلقة.

## الكاميرا

`FreeCamera` ثابتة بمنظور البائع، بدون Inputs (لا كاميرا حرة).
`FOVMODE_HORIZONTAL_FIXED` يضمن بقاء كل العناصر التفاعلية داخل الشاشة،
مع 3 أوضاع حسب نسبة العرض (كمبيوتر/لوحي/هاتف طولي).

## الأداء

- مواد Toon مبسطة (StandardMaterial) مع كاش لكل مشهد.
- Object Pooling للزبائن؛ الجسيمات `disposeOnStop`.
- عدد قطع الإضافات محدود لكل سكوب.
- ثلاثة أوضاع جودة تتحكم في: الظلال (تعطيل/Blur kernel)، ميزانية الجسيمات،
  الغيوم والنوارس، وHardware Scaling. الاختيار الأولي تلقائي من قدرات الجهاز.
- إيقاف حلقة الرندر والصوت والمحاكاة عند انتقال التبويب للخلفية.
- عند إعادة المرحلة يُدمّر المشهد كاملًا (`dispose`) — تحققت اختبارات e2e من
  عدة إعادات متتالية دون أخطاء.

## الصوت

`AudioManager` يولّد كل الأصوات بـWebAudio في وقت التشغيل (جرس، غرف، صوص، عملات،
نجاح/خطأ، موسيقى Chiptune خفيفة، وأمواج) — صفر ملفات صوتية وصفر تراخيص.
يُفتح الـAudioContext من أول إيماءة مستخدم (متطلب iOS).

## الحفظ

`SaveData` بإصدار (`version: 2`) مع `migrate()` (مثال فعلي: v1 كان بحقل `sound`
واحد → v2 بموسيقى/مؤثرات منفصلة)، تعقيم القيم، واسترداد آمن من JSON تالف.
