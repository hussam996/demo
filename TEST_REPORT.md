# تقرير الاختبارات — Test Report

تاريخ التنفيذ: 2026-07-19 · Node v22 · Vitest 3 · Playwright 1.5x (Chromium headless)

## اختبارات الوحدات (Vitest) — ‏47/47 ناجحة ✅

```
✓ src/tests/OrderValidator.test.ts (18 tests)
✓ src/tests/LevelSession.test.ts  (12 tests)
✓ src/tests/Levels.test.ts         (9 tests)
✓ src/tests/SaveManager.test.ts    (8 tests)
Test Files  4 passed (4) · Tests  47 passed (47)
```

### OrderValidator (18)

طلب صحيح كامل · وعاء ناقص · وعاء خاطئ · سكوب ناقص · سكوب زائد · نكهة خاطئة ·
ترتيب خاطئ (مع رصيد جزئي 0.5) · تجاهل الترتيب في الوضع المتساهل · صوص ناقص ·
صوص خاطئ · صوص زائد (صارم/متساهل) · إضافة ناقصة · إضافة زائدة (صارم/متساهل) ·
طلب بلا صوص · طلب متعدد الإضافات · تراكم عدة أخطاء معًا.

### LevelSession — آلة الحالة (12)

بدء المرحلة وعرض الطلب عند الوصول · منع الإضافة قبل الوعاء · منع التفاعل قبل وصول
الزبون · حد السكوبات حسب الوعاء/المستوى · رفض المكونات المقفلة · منع التسليم بمنصة
فارغة · تسليم صحيح (نقاط + مغادرة الزبون + تقدم الطابور) · إعادة المحاولة عند الخطأ
في المستوى المتساهل · نفاد الصبر (مغادرة غاضبة + كسر السلسلة) · إيقاف/استكمال يجمد
الساعة ويعيد الحالة · انتهاء المرحلة بانتهاء الوقت · تدرج الـCombo حتى ×5 وانكساره.

### المستويات (9)

‏8 مستويات بصعوبة متدرجة · المولّد لا يستخدم مكونات مقفلة أبدًا (‏50 عينة × 8 مستويات) ·
حدود عدد السكوبات وسعة الوعاء · الترتيب لا يُفرض قبل المستوى 5 · حجم الطابور يتغير
ولا يتجاوز حد المستوى · احتساب الأهداف · النجاح يفتح المرحلة التالية.

### الحفظ (8)

إنشاء حفظ جديد · حفظ وإعادة تحميل · استرداد من JSON تالف · استرداد من شكل بيانات
خاطئ · Migration من الإصدار 1 إلى 2 · رفض إصدارات مستقبلية والعودة للافتراضي ·
مسح التقدم · الاحتفاظ بأفضل نجوم/نتيجة عند إعادة اللعب.

## اختبارات واجهة اللعب (Playwright) — ‏5/5 ناجحة ✅

تعمل على البناء الإنتاجي (`vite preview`) وتنقر مجسمات المشهد الفعلية عبر إسقاط
إحداثياتها العالمية إلى الشاشة:

1. **بدء اللعب**: قائمة ← خريطة المراحل ← بدء المستوى ← ظهور HUD وبطاقة الطلب،
   **بدون أي أخطاء في Console**.
2. **حلقة كاملة**: نقر الوعاء ثلاثي الأبعاد ← نقر حوض النكهة (حركة المغراف) ←
   نقر الجرس ← تحقق الطلب ← نقاط > 0 وزبون مخدوم.
3. **إيقاف/استكمال** مع بقاء الجلسة سليمة.
4. **إعادة المرحلة 3 مرات متتالية** (فحص تسريب/أخطاء) — لا أخطاء Console.
5. **التجاوب**: هاتف طولي 390×844 وعرضي 844×390 مع بقاء الواجهة ظاهرة.

## البناء

- `npm run build` (يشمل `tsc --noEmit`) ينجح؛ الناتج في `dist/‎` (‏~1.1MB gzip).
- `npx eslint src`: ‏0 أخطاء (تحذير واحد مقصود في ملف اختبار e2e).

## كيفية إعادة التنفيذ

```bash
npm run test                                   # الوحدات
npm run build
PW_CHROMIUM_PATH=<path> npm run test:e2e       # أو npx playwright install chromium
```
